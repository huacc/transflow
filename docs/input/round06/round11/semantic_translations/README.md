# Semantic Translations Directory

This directory is intentionally empty before execution.

Round11 must create:

```text
R11_AIA_zh_pages_03_08_09_24_25.translations.json
```

inside `S5_TranslationPlan`.

The executing Codex must use only extracted Chinese source units and the D2 prompt template. It must not use an English reference PDF, official bilingual PDF, previous round translation JSON, or any prior candidate output as target-language evidence.
