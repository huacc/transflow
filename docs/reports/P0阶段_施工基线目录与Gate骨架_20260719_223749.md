# P0 阶段执行报告：施工基线、目录与 Gate 骨架

| 项 | 内容 |
|---|---|
| 阶段 | P0（P0.1～P0.5） |
| 任务 | 施工基线、目录与 Gate 骨架 |
| 报告生成时间 | 2026-07-19T22:37:49+08:00 |
| 设计输入 | `docs/设计/Transflow_PDF翻译排版引擎_总体设计_v0.1.md` |
| 计划输入 | `docs/计划/Transflow_PDF翻译排版引擎_详细开发计划_v0.1.md` P0 |
| Gate | G0 |
| 最终结论 | **PASS** |

## 1. 执行边界

本次只完成 P0 的工程施工面、可重算基线、测试/Gate 入口、追溯治理和依赖迭代排期。未实现 P1 及之后的虚拟环境冻结、数据库连接、领域合同、PDF 工作流、分类/Toolbox 迁移或 MerqFin 联调。

验证期间使用 `tmp/p0-verify-venv` 作为一次性、被 Git 忽略的证明环境，不把它冒充 P1 的正式环境与锁文件。两个 spike 只读；P0 开始前 Toolbox spike 已有的两处用户工作树修改被写入基线，未被本阶段覆盖或清理。

## 2. 分阶段产出

| 子阶段 | 设计/计划追溯 | 主要产出 | 结果 |
|---|---|---|---|
| P0.1 冻结施工输入 | 设计 §2.2～§2.3、§20.1、§20.5～§20.7、§28；计划 P0.1 | `baseline_manifest.json`、`migration_ledger.json`、可重算脚本 | 通过 |
| P0.2 创建生产目录 | 设计 §19.1～§19.3；计划 P0.2 | `pyproject.toml`、`src/transflow/`、`resources/`、`scripts/`、`tests/`、README 边界 | 通过 |
| P0.3 测试和 Gate 骨架 | 设计 §19.1、§21.1、§24；计划 P0.3 | pytest/coverage/Ruff/Mypy、`run_gate.py`、wheel 审计、快速 CI、Gate 模板及证据 | 通过 |
| P0.4 追溯与决策台账 | 设计 §20.1、§24、§26～§27；计划 P0.4 | `traceability_matrix.json`、`governance_registry.json` | 通过 |
| P0.5 实际排期 | 设计 §22～§23；计划 P0.5 | P1～P14 单通道依赖迭代排期、fixture 盘点、G14 强制停点 | 通过 |

关键文件：

- `scripts/build_p0_assets.py`：从设计、计划、Git 和 spike 原始证据重算 P0 基线、迁移台账与追溯矩阵。
- `scripts/verify_p0.py`：校验目录/包边界、wheel、CI、追溯、治理与排期不变量。
- `scripts/run_gate.py`：按 JSON 清单顺序运行 Gate，首个非零退出立即阻断并保留原始输出；Windows 子进程统一使用 UTF-8。
- `tests/test_p0.py`：15 个计划 ID 对应的真实仓库数据测试；输入故障夹具只用于验证拒绝路径，输出与退出码均由真实命令产生。
- `resources/manifests/gate_catalog.json`：G0 权威命令清单。
- `docs/reports/gates/G0_evidence.json`：G0 完整 stdout、stderr、命令、耗时和退出码证据。

## 3. Gate G0 逐项验收

| # | 结论 | 量化结果 | 证据 |
|---|---|---|---|
| G0-1 输入基线 | 通过 | 55 个迁移单元；缺失字段 0；重复 ID 0；三类基线可重算；两个 spike 与 P0 冻结时一致 | E1、E2 |
| G0-2 目录与包边界 | 通过 | `transflow` 仅从 `src/transflow` 导入；空业务壳、spike/MerqFin import 违规 0；真实 wheel 禁止材料 0 | E1、E3；P0.2-T01～T03 |
| G0-3 测试/Gate/CI | 通过 | G0 五个步骤退出码全为 0；失败注入真实退出码 9 并阻断；15/15 测试通过；覆盖率 90.03% ≥ 85% | E1；P0.3-T01～T04 |
| G0-4 追溯与决策 | 通过 | 127 个任务、212 个 Gate 行、686 个测试 ID；无效设计引用、悬空测试、无归属测试均为 0 | E2、E3 |
| G0-5 排期依赖 | 通过 | P1～P14 共 14 阶段，单执行通道；排期违规 0；G14 强制停点为 true；P15～P23 无固定发布日期 | E2、E3 |
| G0-6 待决策 | 通过 | 阻塞 P1 的 open decision 数量为 0；P0 治理状态为 PASS | E2、E3 |

## 4. 实际命令证据

### E1：权威 G0 结果

执行命令：

```powershell
$python = Join-Path (Get-Location) 'tmp\p0-verify-venv\Scripts\python.exe'
& $python scripts/run_gate.py G0 --report docs/reports/gates/G0_evidence.json
```

从该命令写出的原始 JSON 证据中读取汇总：

```powershell
$gate = Get-Content -Raw -Encoding UTF8 docs/reports/gates/G0_evidence.json | ConvertFrom-Json
$tests = $gate.steps | Where-Object step_id -eq 'G0-TESTS'
[pscustomobject]@{gate=$gate.gate; conclusion=$gate.conclusion; finished_at=$gate.finished_at; step_return_codes=($gate.steps | ForEach-Object { "$($_.step_id)=$($_.return_code)" }) -join ','; pytest_summary=[regex]::Match($tests.stdout, '\d+ passed in [0-9.]+s').Value; coverage_summary=[regex]::Match($tests.stdout, 'Total coverage: [0-9.]+%').Value} | ConvertTo-Json
```

实际输出：

```json
{
  "gate": "G0",
  "conclusion": "PASS",
  "finished_at": "2026-07-19T22:25:47+08:00",
  "step_return_codes": "G0-STATIC=0,G0-TYPES=0,G0-BASELINE=0,G0-BOUNDARIES=0,G0-TESTS=0",
  "pytest_summary": "15 passed in 37.65s",
  "coverage_summary": "Total coverage: 90.03%"
}
```

### E2：基线、追溯、决策、排期与 spike 未改动

执行命令（只读重算并读取治理资产）：

```powershell
$python = Join-Path (Get-Location) 'tmp\p0-verify-venv\Scripts\python.exe'
& $python -c "import json; from scripts import build_p0_assets as a, verify_p0 as v; b=json.loads(a.BASELINE_PATH.read_text(encoding='utf-8')); c=a.collect_baseline_manifest(); l=json.loads(a.LEDGER_PATH.read_text(encoding='utf-8')); t=json.loads(a.TRACEABILITY_PATH.read_text(encoding='utf-8')); g=json.loads(v.GOVERNANCE_PATH.read_text(encoding='utf-8')); s=json.loads(v.SCHEDULE_PATH.read_text(encoding='utf-8')); print(json.dumps({'baseline_equal':c==b,'classification_spike_unchanged':c['classification_spike']==b['classification_spike'],'toolbox_spike_unchanged':c['toolbox_spike']==b['toolbox_spike'],'migration_units':len(l['units']),'duplicate_ids':len(l['units'])-len({u['unit_id'] for u in l['units']}),'trace_tasks':len(t['tasks']),'trace_gate_rows':len(t['gate_rows']),'trace_test_ids':len(t['indexes']['by_test_id']),'trace_validation':t['validation'],'stage_status':g['current_stage']['status'],'open_decisions':len(g['current_stage']['open_decision_ids']),'p1_p14_stages':len(s['first_part']),'g14_stop':s['g14_stop']['required']},ensure_ascii=False,indent=2))"
```

实际输出：

```json
{
  "baseline_equal": true,
  "classification_spike_unchanged": true,
  "toolbox_spike_unchanged": true,
  "migration_units": 55,
  "duplicate_ids": 0,
  "trace_tasks": 127,
  "trace_gate_rows": 212,
  "trace_test_ids": 686,
  "trace_validation": {
    "dangling_gate_test_references": [],
    "invalid_design_references": [],
    "unowned_test_definitions": []
  },
  "stage_status": "PASS",
  "open_decisions": 0,
  "p1_p14_stages": 14,
  "g14_stop": true
}
```

### E3：最终不变量与路径规则

执行命令：

```powershell
$python = Join-Path (Get-Location) 'tmp\p0-verify-venv\Scripts\python.exe'
& $python -m scripts.verify_p0 all
```

实际输出：

```json
{
  "baseline": [],
  "ci": [],
  "governance": [],
  "package_boundary": [],
  "schedule": [],
  "traceability": []
}
```

执行命令：

```powershell
rg --pcre2 -n "(?i)(?<![a-z])[a-z]:[\\/]" src scripts tests .github pyproject.toml resources docs/合同 docs/迁移 docs/计划/Transflow_P1-P14_依赖迭代排期_v0.1.json
if ($LASTEXITCODE -eq 1) { Write-Output 'ABSOLUTE_DRIVE_PATH_MATCHES=0'; exit 0 }
```

实际输出（`rg` 无命中时由命令包装输出）：

```text
ABSOLUTE_DRIVE_PATH_MATCHES=0
```

## 5. 执行中发现并关闭的问题

1. 首轮测试 15 个用例全部通过，但脚本覆盖率仅 64.04%，Gate 正确失败。未降低 85% 门槛；补充了 Gate 成功/失败、路径逃逸、损坏清单、wheel 越界/缺失、CI/追溯/治理/排期拒绝路径的真实测试，最终覆盖率为 90.03%。
2. 首次正式 G0 在 Windows 转发 pytest 输出时出现编码不一致并触发 `UnicodeEncodeError`。已在 Gate 子进程边界统一 UTF-8，失败退出码透传测试与正式 G0 均重新通过。

上述问题均已关闭，没有遗留 `[待决策]`。P0 已满足进入 P1 的 Gate 条件，但本报告不授权跳过 P1 自身计划和 G1。
