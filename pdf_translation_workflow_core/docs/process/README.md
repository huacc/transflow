# Core Process Documents

本目录保存 core 的稳定流程设计文档。

当前核心文档：

- `PDF_语义翻译回填_标准流程设计.md`

该文件从 `docs/业务流程/PDF_语义翻译回填_标准流程设计.md` 复制而来。旧位置暂时保留，避免破坏已有引用。

后续如果流程设计发生变化，应保持两点：

- core 侧文档必须能指导独立 Codex 按状态机、工具契约和门禁执行。
- 任何从 lab round 合入的新能力，都要在本目录或 `../promotion/` 中留下合入说明。
