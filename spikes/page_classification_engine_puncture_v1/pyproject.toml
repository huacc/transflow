[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "page-classification-engine-puncture-v1"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["httpx", "PyMuPDF"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
