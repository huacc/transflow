# round10

Purpose: validate whether the current workflow documentation, contracts, prompts, and tools can guide a fresh Codex session through the PDF semantic backfill state machine.

Start here:

```text
docs\测试提示词\ROUND10_ENGINE_WORKFLOW_VALIDATION_PROMPT.md
```

The execution Codex should run from this directory and write all outputs under:

```text
docs\output
docs\reports
```

The expected final report is:

```text
docs\reports\round10_execution_audit.md
```
