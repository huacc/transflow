# {{TOOLBOX_KEY}} 工具箱

当前成熟度：`EXPERIMENTAL`。

本目录只处理 `{{TOOLBOX_KEY}}`。分类、翻译 Provider 和公共 PDF 机械能力不在此目录实现。

执行顺序：画像与样本 → development 探索 → 流程冻结 → 固定译文 → 千问端到端 → regression → holdout → Gate → PromotionManifest。

