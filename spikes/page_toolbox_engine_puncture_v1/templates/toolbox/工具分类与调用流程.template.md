# {{TOOLBOX_KEY}}：工具分类与调用流程

## 工具清单

| 工具 | 类别 | 输入 | 输出 | 失败语义 |
|---|---|---|---|---|

## 固定流程

```text
PageFacts
→ PageTemplateBuilder
→ PageTranslationRequest
→ TranslationProvider
→ LayoutPlanner
→ SharedPdfKernel
→ ToolboxJudge
→ 可选 RepairPolicy
→ 完整复裁
```

## Codex 探索记录

只记录 development 阶段的工具实验；流程冻结后不得作为正式运行时自由分支。

## 流程冻结条件

列出从 `EXPERIMENTAL` 进入 `REGRESSION` 的证据。

