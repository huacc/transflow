# Core Documentation

本目录保存 `pdf_translation_workflow_core` 的稳定设计文档和合入说明。

## 子目录

- `process/`：主流程设计、状态机、活动流、裁决逻辑等稳定文档。
- `promotion/`：从 lab round 合入 core 的迁移记录、检查清单和验收说明。

原始业务流程文档仍保留在 `docs/业务流程/` 下。本目录中的副本用于 core 侧实现和后续 spike 验证。
